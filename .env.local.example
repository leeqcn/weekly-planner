# 复制成 .env.local（已被 .gitignore 忽略）。
# 填日记 App 同一个 Supabase 实例的值 —— 共用实例，但这是独立的一套表和独立的 Vercel Project。
# 两个都留空也能跑：App 会进「本地模式」，数据存浏览器 localStorage。
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# 上面填好了、但暂时还想用本地假数据调 UI 时，把这行取消注释。
# VITE_USE_MOCK=1
